diff --git a/yolo_ros/yolo_ros/tracking_node.py b/yolo_ros/yolo_ros/tracking_node.py
index 80dde15..78da331 100644
--- a/yolo_ros/yolo_ros/tracking_node.py
+++ b/yolo_ros/yolo_ros/tracking_node.py
@@ -14,6 +14,9 @@
 # along with this program.  If not, see <https://www.gnu.org/licenses/>.
 
 
+import json
+import time
+
 import rclpy
 from rclpy.qos import QoSProfile
 from rclpy.qos import QoSHistoryPolicy
@@ -35,6 +38,7 @@ from ultralytics.utils import IterableSimpleNamespace, YAML
 from ultralytics.utils.checks import check_requirements, check_yaml
 
 from sensor_msgs.msg import Image
+from std_msgs.msg import String
 from yolo_msgs.msg import Detection
 from yolo_msgs.msg import DetectionArray
 
@@ -80,6 +84,8 @@ class TrackingNode(LifecycleNode):
 
         self.tracker = self.create_tracker(tracker_name)
         self._pub = self.create_publisher(DetectionArray, "tracking", 10)
+        self._timing_pub = self.create_publisher(String, "tracking_timing", 10)
+        self._last_tracking_cb_time = None
 
         super().on_configure(state)
         self.get_logger().info(f"[{self.get_name()}] Configured")
@@ -211,10 +217,25 @@ class TrackingNode(LifecycleNode):
 
         tracked_detections_msg = DetectionArray()
         tracked_detections_msg.header = img_msg.header
+        t0 = time.perf_counter()
+        now_msg = self.get_clock().now().to_msg()
+        image_age_ms = (
+            (now_msg.sec - img_msg.header.stamp.sec) * 1000.0
+            + (now_msg.nanosec - img_msg.header.stamp.nanosec) / 1.0e6
+        )
+        detection_age_ms = (
+            (now_msg.sec - detections_msg.header.stamp.sec) * 1000.0
+            + (now_msg.nanosec - detections_msg.header.stamp.nanosec) / 1.0e6
+        )
+        callback_interval_ms = None
+        if self._last_tracking_cb_time is not None:
+            callback_interval_ms = (t0 - self._last_tracking_cb_time) * 1000.0
+        self._last_tracking_cb_time = t0
 
         # Convert image
         cv_image = self.cv_bridge.imgmsg_to_cv2(img_msg, desired_encoding="bgr8")
         cv_image = cv2.cvtColor(cv_image, cv2.COLOR_BGR2RGB)
+        t_convert = time.perf_counter()
 
         # Parse detections
         detection_list = []
@@ -231,12 +252,14 @@ class TrackingNode(LifecycleNode):
                     detection.class_id,
                 ]
             )
+        t_parse = time.perf_counter()
 
         # Tracking
         if len(detection_list) > 0:
 
             det = Boxes(np.array(detection_list), (img_msg.height, img_msg.width))
             tracks = self.tracker.update(det, cv_image)
+            t_update = time.perf_counter()
 
             if len(tracks) > 0:
 
@@ -260,9 +283,38 @@ class TrackingNode(LifecycleNode):
 
                     # Append msg
                     tracked_detections_msg.detections.append(tracked_detection)
+            t_build = time.perf_counter()
+        else:
+            t_update = time.perf_counter()
+            t_build = t_update
 
         # Publish detections
         self._pub.publish(tracked_detections_msg)
+        t_publish = time.perf_counter()
+
+        timing_msg = String()
+        timing_msg.data = json.dumps(
+            {
+                "stamp": self.get_clock().now().nanoseconds / 1.0e9,
+                "image_age_ms": image_age_ms,
+                "detection_age_ms": detection_age_ms,
+                "callback_interval_ms": callback_interval_ms,
+                "image_width": img_msg.width,
+                "image_height": img_msg.height,
+                "input_detection_count": len(detections_msg.detections),
+                "tracked_detection_count": len(tracked_detections_msg.detections),
+                "timing_ms": {
+                    "image_convert": (t_convert - t0) * 1000.0,
+                    "parse_detections": (t_parse - t_convert) * 1000.0,
+                    "tracker_update": (t_update - t_parse) * 1000.0,
+                    "build_output": (t_build - t_update) * 1000.0,
+                    "publish": (t_publish - t_build) * 1000.0,
+                    "total": (t_publish - t0) * 1000.0,
+                },
+            },
+            separators=(",", ":"),
+        )
+        self._timing_pub.publish(timing_msg)
 
 
 def main():
diff --git a/yolo_ros/yolo_ros/yolo_node.py b/yolo_ros/yolo_ros/yolo_node.py
index da6dba0..16bed7e 100644
--- a/yolo_ros/yolo_ros/yolo_node.py
+++ b/yolo_ros/yolo_ros/yolo_node.py
@@ -14,6 +14,8 @@
 # along with this program.  If not, see <https://www.gnu.org/licenses/>.
 
 
+import json
+import time
 from typing import List, Dict
 from cv_bridge import CvBridge
 
@@ -35,6 +37,7 @@ from ultralytics.engine.results import Keypoints
 
 from std_srvs.srv import SetBool
 from sensor_msgs.msg import Image
+from std_msgs.msg import String
 from yolo_msgs.msg import Point2D
 from yolo_msgs.msg import BoundingBox2D
 from yolo_msgs.msg import Mask
@@ -143,7 +146,9 @@ class YoloNode(LifecycleNode):
         )
 
         self._pub = self.create_lifecycle_publisher(DetectionArray, "detections", 10)
+        self._timing_pub = self.create_lifecycle_publisher(String, "timing", 10)
         self.cv_bridge = CvBridge()
+        self._last_image_cb_time = None
 
         super().on_configure(state)
         self.get_logger().info(f"[{self.get_name()}] Configured")
@@ -238,6 +243,7 @@ class YoloNode(LifecycleNode):
         self.get_logger().info(f"[{self.get_name()}] Cleaning up...")
 
         self.destroy_publisher(self._pub)
+        self.destroy_publisher(self._timing_pub)
         del self.image_qos_profile
 
         super().on_cleanup(state)
@@ -434,11 +440,22 @@ class YoloNode(LifecycleNode):
         """
 
         if self.enable:
+            t0 = time.perf_counter()
+            now_msg = self.get_clock().now().to_msg()
+            image_age_ms = (
+                (now_msg.sec - msg.header.stamp.sec) * 1000.0
+                + (now_msg.nanosec - msg.header.stamp.nanosec) / 1.0e6
+            )
+            callback_interval_ms = None
+            if self._last_image_cb_time is not None:
+                callback_interval_ms = (t0 - self._last_image_cb_time) * 1000.0
+            self._last_image_cb_time = t0
 
             # Convert image + predict
             cv_image = self.cv_bridge.imgmsg_to_cv2(
                 msg, desired_encoding=self.yolo_encoding
             )
+            t_bridge = time.perf_counter()
             results = self.yolo.predict(
                 source=cv_image,
                 verbose=False,
@@ -453,7 +470,14 @@ class YoloNode(LifecycleNode):
                 retina_masks=self.retina_masks,
                 device=self.device,
             )
+            t_predict = time.perf_counter()
             results: Results = results[0].cpu()
+            t_cpu = time.perf_counter()
+
+            hypothesis = []
+            boxes = []
+            masks = []
+            keypoints = []
 
             if results.boxes or results.obb:
                 hypothesis = self.parse_hypothesis(results)
@@ -464,6 +488,7 @@ class YoloNode(LifecycleNode):
 
             if results.keypoints:
                 keypoints = self.parse_keypoints(results)
+            t_parse = time.perf_counter()
 
             # Create detection msgs
             detections_msg = DetectionArray()
@@ -490,6 +515,35 @@ class YoloNode(LifecycleNode):
             # Publish detections
             detections_msg.header = msg.header
             self._pub.publish(detections_msg)
+            t_publish = time.perf_counter()
+
+            timing_msg = String()
+            timing_msg.data = json.dumps(
+                {
+                    "stamp": self.get_clock().now().nanoseconds / 1.0e9,
+                    "image_age_ms": image_age_ms,
+                    "callback_interval_ms": callback_interval_ms,
+                    "image_width": msg.width,
+                    "image_height": msg.height,
+                    "encoding": msg.encoding,
+                    "configured_imgsz": [self.imgsz_height, self.imgsz_width],
+                    "half": self.half,
+                    "max_det": self.max_det,
+                    "threshold": self.threshold,
+                    "detection_count": len(detections_msg.detections),
+                    "ultralytics_speed_ms": getattr(results, "speed", {}),
+                    "timing_ms": {
+                        "bridge": (t_bridge - t0) * 1000.0,
+                        "predict": (t_predict - t_bridge) * 1000.0,
+                        "cpu": (t_cpu - t_predict) * 1000.0,
+                        "parse": (t_parse - t_cpu) * 1000.0,
+                        "publish": (t_publish - t_parse) * 1000.0,
+                        "total": (t_publish - t0) * 1000.0,
+                    },
+                },
+                separators=(",", ":"),
+            )
+            self._timing_pub.publish(timing_msg)
 
             del results
             del cv_image
